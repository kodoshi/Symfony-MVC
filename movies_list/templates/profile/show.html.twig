
{% extends 'base.html.twig' %}

{% block title %}My profile{% endblock %}

{% block body %}
    <div class="container w-50 mt-3 border col">
        <h1>Hello {{ user.name }}</h1>

        <table class="table mt-3 row">
            <tbody>
            <tr>
                <th style="width:200px;">User n° : </th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email : </th>
                <td>{{ user.email }}</td>
            </tr>
                        <tr>
                <th>Name : </th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Roles : </th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            </tbody>
        </table>
        <div class="d-flex justify-content-between mb-3">
        <a class="btn btn-secondary" href="{{ path('profile_edit', {'id' : user.id}) }}"> Edit profile </a>
        </div>
        <div class="d-flex justify-content-between mb-3">
        <a class="btn btn-secondary" href="{{ path('profile_delete', {'id' : user.id}) }}" onsubmit="return confirm('Are you sure ?')"> Delete profile </a>
        </div>
{% endblock %}