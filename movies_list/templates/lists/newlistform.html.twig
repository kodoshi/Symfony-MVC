{{ form_start(form) }}
        {{ form_errors(form) }}
<div class="mt-3">
    {{ form_label(form.name, 'List Name') }}
    {{ form_errors(form.name) }}
    {{ form_widget(form.name) }}
    {{ form_help(form.name) }}
</div>

<div class="mt-3">{{ form_row(form.description) }}</div>

<div class="d-flex justify-content-between mb-3">
    <button class="btn btn-secondary">Save List</button>

{{ form_end(form) }}